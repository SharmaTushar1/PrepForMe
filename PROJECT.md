# PrepFor.Me — Source of Truth

*The canonical project doc: what it is, how it works, what's decided, and what's next. Kept concise but complete. Supersedes the standalone decision log. Version history at the bottom (major changes only, moving forward).*

**Live:** https://prep-for-me.vercel.app/ (currently a basic CRUD skeleton — build in progress)
**Stage:** pre-v1, targeting 10–20 friends-and-family testers.
**Technical detail:** [TECHNICAL.md](TECHNICAL.md) — stack, schema, hosting, deploy, gotchas.
**Last updated:** 7 Aug 2026 (prod PDF render fix)

---

## Current state

What exists in the repo today, as distinct from what's planned below.

**Working, persisted, multi-user:** magic-link and Google OAuth sign-in; Postgres schema with row level security on every table (including `prep_chunks` / `ai_usage`); routed URLs so a refresh lands where you were; profile and settings editing; adding and editing roles (with `company_domain`); logging recaps with real text; funnel metrics derived from actual rows; JSON export and per-application corpus clearing; empty states throughout.

**Base resume, new:** a PDF uploaded from onboarding or the profile lands in a private per-user Storage bucket and stops there. Reading it is a second, explicit press — the analysis is the only thing in the product that costs money, so it is never triggered by choosing a file — and one call then returns both a seven-category ATS report and a structured parse of the resume. A run that dies without reporting back is escapable rather than an eternal spinner: after three minutes the screen says so and offers the button again. The parse is shown for review with a checkbox per role, bullet and skill, and reaches the profile spine only when the user confirms — so tailoring finally has real bullets behind it. This closes the front door that onboarding never had: until now the only way onto the profile was typing it in by hand. The analysis reports its own progress while it runs — four real steps and, through the long model call, a clock rather than a fake percentage — and the whole path has now run for real against Claude. Upload copy states that output is always a **template PDF**, never a restyle of the original file.

**Generate / Tailor PDF (templates, 5 Aug):** hand-rolled PDF/DOCX rebuild is retired for this path. Profile **Generate resume** and application **Materials → Tailor** share one pipeline: structured `ResumeFields` → Classic or Compact HTML → **Vercel Chromium PDF** (`/api/render-resume-pdf`). Storage: `profiles.default_template_id`, `applications.template_id` override, `applications.tailored_resume` jsonb (fields plus the last tailor session — summary, diffs, skill-gap prompts and drafts — so leaving Materials does not force a re-spend). The profile spine now also holds phone/location/links/summary and education/projects/certifications (`0011`), which onboarding confirm writes and `tailor-resume` reads — previously those were parsed then dropped, so every tailored PDF was missing them. Tailor uses Edge Function `tailor-resume` (quota feature `tailor`) with per-item JD relevance weighting, skill-gap elicitation (brief or skip → skill chip + 1–2 bullets on the application copy only; briefs add, never crowd out), and a content-fidelity check vs the profile spine before download. After a tailor, **Tweak this version** takes a free-text follow-up ("change my email…") and mutates only what was asked — not a full re-tailor. Opening the tab restores the saved pass; **Re-tailor** and **Apply edit** each spend an allowance (with confirm). DOCX download and the four paid templates are out of scope for v1. See [§14](#14-resume-templates-generate--tailor).

**Rewrites, new:** the report no longer only names weak bullets — it offers to write better ones. One press rewrites every line worth rewriting in a single model call (**about $0.02–0.04**), and each suggestion appears under the finding it answers, original above and rewrite below, to accept or dismiss one at a time or accept in bulk. Where a bullet needs a number the resume never stated, the rewrite leaves a blank to fill rather than a figure to defend — and if a suggestion does introduce a number of its own, it is flagged and held back from "accept all" so it can only be kept deliberately. Accepted rewrites feed the template PDF and nothing else; the profile is untouched. **Run for real: about a cent a pass, and the rewrite it returned was exactly right.** The first pass could only reach one line because the report it worked from had a parse that dropped several of the resume's bullets — an analyzer bug the rewrite pass exposed rather than caused, since fixed. Re-run over a complete parse it again returned one rewrite, and that time correctly: of seventeen findings only one asked for different words, the rest being passes or problems no rewrite can fix. See [§15](#15-rewriting-the-bullets-built-costs-a-model-call).

**Deployed and verified:** Vercel, building from GitHub `main`, credentials inlined at build time, deep links served correctly. Supabase project in Mumbai (`ap-south-1`), the first three migrations applied, sign-up confirmed end to end — accounts are created and the trigger seeds their `profiles` and `user_settings` rows.

**Working, as of the third attempt:** the model. `analyze-resume` is the project's first Supabase Edge Function — Anthropic Claude, key server-side, a per-user monthly allowance and an in-flight lock bounding a runaway client — and it now returns a real report. On a one-page resume, latest run: **overall 85, seven scored categories, seventeen findings, four roles with nine bullets, one degree, five projects and thirty skills parsed, in 80 seconds for about $0.10.** The findings are specific enough to be worth reading — it named the one bullet with no measurable outcome, spotted a skill listed twice under two different headings, and noticed the resume never states where its author lives. Getting there cost two failures: a schema whose repeated category objects compiled into a decoding grammar Anthropic refuses outright, then a `max_tokens` budget sized for the answer alone, which Sonnet 5's default adaptive thinking spent on reasoning before truncating the report.

**Two more failures came later, and both changed the design.** Adding three parse sections put the schema back over the grammar ceiling — repetition again, fixed by carrying education, projects and certifications as one array tagged by `kind`. And the streamed progress bar, twelve honest milestones, turned out to be unaffordable: the edge runtime charges CPU per frame of the model's response, a thinking-heavy answer arrives in thousands of them, and a real analysis was killed mid-answer at 84 seconds with the tokens already spent. Our own code accounted for 365 ms of that budget. So the analysis is one non-streaming call now, reporting four real steps with a clock through the long middle, while the rewrite pass — whose answers are a few hundred frames — still streams a genuinely moving bar. All of it is provable for free against a local stub that can be told to be slow, and all of it then held on a real run: see [TECHNICAL.md §8](TECHNICAL.md#8-the-ai-seam).

**Spending is real now, and the cap still isn't set.** `.env.development.local` sets `VITE_AI_PROVIDER=edge` and the Anthropic key sits in `supabase/.env.local`, so on this machine pressing "Analyze resume" bills a real key with nothing behind it. Two hundred milli-dollars across three runs is a rounding error; the reason it is written down is that half of it bought nothing, in code that had been read and never run, and the in-code guards do not bound a bill — the daily cap counts *saved* reports, so a call that gets billed and then fails never moves the counter. What is still true is narrower than it was: nothing spends without an explicit press, and an environment that has not opted in, Vercel included, still gets the local provider, whose every report is a labelled sample carrying an on-screen "no model was called" banner and is never written to the database.

**Company prep RAG, new:** paste notes, URL (robots.txt gate), or PDF → extract restated atomic claims (never verbatim page text) → embed with OpenAI → retrieve in prep chat with provenance. Chat is multi-turn (last 8 messages), citations are clickable, Save to prep suggests claims from the exchange. Company facts from first-party/news can share immediately; interview claims stay private until candidate corroboration. Prep panel counts company-scope sibling sources and shared claims. **Catalog-first add-role** (typeahead company/role, generic levels, specialty/employment type) feeds stable prep slugs and cleaner LinkedIn search. See §16 and [PHASE3_SPEC.md](PHASE3_SPEC.md).

**Still not real:** JD tailoring and referral drafts still run the local mock. Also absent: Discover's job feeds, Practice, the browser extension. Each says so on screen rather than pretending.

**Scale caveat:** single-user-per-account, no test suite, no CI, no linter, one 677 kB bundle. Appropriate for 10–20 testers, not beyond.

---

## 1. What it is

A **profession-agnostic** job-search copilot (tech and non-tech). A user uploads a resume, gets a structured profile and role advice, tracks applications, tailors materials truthfully to each job, and — the differentiator — gets **deep, company-and-role-specific interview prep that compounds over time.**

**Positioning:** prep-first, not volume-first. Apply to fewer jobs, better; walk in knowing the company cold. Never overpromises a job or cites false "Nx your chances" multipliers.

**Name/domain:** PrepFor.Me (live). Doesn't block anything.

## 2. The moat (core insight)

The moat is **not** automation (commoditized, legally fraught). It's the **per-application prep space + the debrief loop**, and specifically the **first-party interview data users contribute** — legal, defensible, and it *survives churn* (users leave on success, but their data stays and improves the product for the next person). The compounding and the churn-defense are the same asset.

- **One coach, many dossiers:** one AI model, scoped context per application. Feels like a specialist per company; costs almost nothing extra (stored embeddings + scoped retrieval via metadata filter / pgvector).
- **Three knowledge layers**, composed at query time:
  1. **Company layer** — public first-party info (site, blog, product, news, funding). Shared across roles at that company.
  2. **Role & level layer** — what the interview looks like for that role/level. Also strong at the **interview-type** level (behavioral, system design, case, sales roleplay) — this is where a user's notes compound across companies, since you rarely re-interview at the same company but face the same *format* repeatedly.
  3. **Personal layer** — the user's own interview notes ("debriefs"). Private.
- **Compounding loop:** after each real interview the user logs notes → deepens that prep space. The "gets smarter with use" magic; it's the user's own data → zero ToS/legal risk.
- **UI naming:** plain words — "Company prep" and "Interview notes" (not "dossier"/"debrief" — those are internal metaphors).

## 3. Data sourcing & integrity rules (decided)

- **Seed content:** permissive-licensed (MIT / Apache / CC-BY / CC0) **or** originally generated. **Verify the license at the source, not just the label** — permissive tags on HuggingFace/GitHub are frequently wrong (license may cover packaging, not content).
- **Never ingest** LeetCode, LintCode, Glassdoor, or other paywalled/competitor corpora (copyright + ToS landmine — and unnecessary). Detection risk scales *with success*, so it's a liability exactly when it would start to matter.
- **Coding problems:** describe classic patterns in your own words (patterns aren't ownable); **link out** to sites rather than reproducing their text. Generic textbook problems = fine verbatim; custom/company-specific ones = own words only.
- **Cold-start:** day-one baseline = model's general knowledge + real public company info + user concentration (cluster early users by city/industry/company so debriefs overlap and the flywheel ignites). No scraped corpus needed. Non-tech has *no* clean company-specific open dataset — same play, company-specific layer always starts thin and fills via users.
- **Hard line:** generate general/format material freely; **never present fabricated company-specific facts, even hedged.** "Low confidence" ≠ "we invented this." AI guesses about a specific company must be labeled *"AI-inferred, unconfirmed,"* not dressed as low-confidence intel. Generated content lives in the general layer only.

## 4. Scope

**v1 — build fully**
- Onboarding: resume → parsed **structured profile** (discrete editable fields, not text blobs).
- Profile view/edit + standing strengths/gaps review.
- **Application tracker** (the spine): Saved → Applied → Screen → Interview → Final/Onsite → Offer; + Rejected/Withdrawn; customizable.
- **Resume tailoring:** paste JD → truthful reframe (never fabricates) + ATS keyword-gap analysis. Show what changed and why (transparency = trust). *Lower priority than the differentiator — see §9.*
- **Company prep workspace:** per-application scoped RAG chat with source provenance.
- **Interview notes capture:** quick structured log; visibly strengthens the prep space.

**v2 — convenience & reach**
- Browser extension (autofill in the user's own session; human submits).
- Job discovery: pull roles from public ATS feeds, semantically rank vs profile.

**Later / premium**
- Mock interview (AI interviewer grounded in company context).
- Evaluation engine (rubric-based answer scoring — flagship premium).
- Practice library (profession-adaptive drills).

**Getting-the-interview layer** (build into product): speed alerts on fresh postings; company-specific "why this company" notes; truthful ATS optimization + fit scoring; activate the user's *own* network; personalized consented follow-up (user sends from own account). Referral network = later phase, not v1.

## 5. Out of scope (do not build)

- **Auto-submit applications** (ATS endpoints gated by employer keys; headless automation = brittle + CAPTCHA + ToS breach) → reframe as browser-extension autofill.
- **Scraping recruiters/emails** (LinkedIn ToS bans the user's account; DPDPA/GDPR liability) → use the posting itself + user's own network + consented outreach.
- **False multiplier claims / reach guarantees.**

## 6. Confidence feature (good idea — build it)

Per company/scope confidence level, rising as data accrues (Glassdoor-style). **Driven by independent corroboration, not raw volume** (multiple candidate reports agreeing → high; defends against gaming). **Always paired with a provenance/source label** — "from company site," "from 3 candidate reports," "general pattern," "AI-inferred, unconfirmed." Source is what the user weighs; a bare number isn't honest.

## 7. Tech stack

Vite SPA (React) on **Vercel** + **Supabase** (Postgres + pgvector + Auth + Storage) + LLM (Anthropic/OpenAI behind one swappable `generate()` interface; default cheap tier, premium only for prep chat) + OpenAI `text-embedding-3-small` + **Upstash** (jobs/queue) + **Resend** (email) + **Sentry** + **PostHog** + GitHub. Job postings via Greenhouse/Lever/Ashby public JSON + Adzuna. Extension via Plasmo/Manifest V3. Payments later: Razorpay.

**Critical build rules:** LLM calls **server-side only** (never key in frontend); set a **hard spend cap + billing alert ($20–30)** before any real call; keep the LLM behind one swappable interface.

**Supabase settings:** Region Mumbai (ap-south-1, permanent); Data API ON; auto-expose new tables OFF; **automatic RLS ON** (app holds resumes + interview notes → every table locked to "users see only their own rows").

**Consequence of "auto-expose OFF" (learned the hard way):** since 2026-05-30 Supabase does not grant new tables to the Data API roles, so **every migration that creates a table must also grant it** to `authenticated`, or every request returns `42501 permission denied` with policies that look perfectly correct. Grants are checked before RLS, so the error mentions nothing about policies. Pattern lives in `supabase/migrations/0003_grants.sql`; full explanation in [TECHNICAL.md §7](TECHNICAL.md#7-migrations-and-the-grants-rule).

Implementation detail for everything in this section — versions, project refs, env vars, deploy pipeline — lives in [TECHNICAL.md](TECHNICAL.md). Of the stack above, Vite, React, Vercel and Supabase are in use, the last of those now including Storage and Edge Functions as well as Postgres and Auth. Anthropic is called for real, and its monthly spend cap is now set — alongside a per-user allowance in the app, since the cap bounds the account and the allowance bounds each person who signs up. pgvector, OpenAI embeddings, Upstash, Resend, Sentry and PostHog are planned, not installed.

## 8. Cost (month 1, 10–20 testers)

Mostly free tiers; only LLM scales with use. One-time: domain + Chrome Web Store $5. LLM on cheap tier ≈ ₹200 total for 20 users; on premium-for-everything ≈ ₹4,400 (~20x gap). **Under ₹6,000/month** if prep chat defaults to a mini model + spend cap is set.

**Measured so far, per user, real calls rather than estimates:** one resume analysis ≈ **$0.09–0.10** on `claude-sonnet-5` (70–80s, and over half of it thinking tokens nobody reads), one rewrite pass ≈ **$0.013** (~10s). Both at Anthropic's introductory $2/$10 per MTok; the same two runs become about **$0.15 and $0.02** at the $3/$15 rates from 1 Sep. Both are one-press, one-call, and neither repeats unless asked — so a tester who uploads once and rewrites once costs about **$0.10**, of which the analysis is nine tenths. Twenty of them is under $3, which is why the uncapped account is an ordering mistake rather than a budget problem.

**What the failures cost is the more useful number.** Of six real analysis calls, three produced nothing and two of those three were billed anyway — once for a report truncated by its own thinking, once for a run the runtime killed at 84 seconds. Roughly fifteen cents for nothing. Every cause was then found for free against the local stub, which is why the stub can now be told to wait ninety seconds or to emit four thousand frames: the failures that cost money are the slow ones, and a stub that answers instantly cannot reproduce them.

## 9. Strategic read (context for decisions)

- Demand is real and people pay, but the category is **saturated** and job-seeker B2C **churns by design** (success = they leave → low LTV).
- **Strongest as a portfolio + personal tool** for the founder's own job switch (dogfoods, demonstrates RAG/full-stack/eval skill). **As a business, only via a wedge:** India-specific (US tools are Greenhouse/ATS-centric; India underserved) *or* B2B (bootcamps/universities/outplacement — buyer isn't the churner).
- Durable value = the un-sexy differentiated infrastructure (debrief-fed corpus, evals), not flashy automation. Build the prep spine first.
- **Goal stated:** ~$10K/mo MRR eventually. Churn-tolerant, but that requires a cheap repeatable acquisition channel (MRR leaks as fast as it enters). Deferred until there are users.

## 10. Design / UX principles

Prep over volume; truthful by design; human always acts (never auto-submits/sends/scrapes); make compounding *felt* ("prep space levels up"); calm-under-pressure tone (war room / coach's notebook, not cheerful startup or cold enterprise); one coach many dossiers; provenance = trust (show where prep came from); every screen has empty/loading/populated/error states; **avoid generic-AI defaults** (esp. cream + serif + terracotta). Application detail screen = the heart (materials + company prep + interview notes per role).

## 11. Legal / non-functional

- **Single-user first** (one candidate per account); harden to multi-tenant only if earned.
- **DPDPA (India):** holds sensitive career data + PII → encryption, purpose limitation, deletion-on-request, prominent privacy controls; RLS enforces per-user isolation.
- Company corpus sharing: **first-party domain + news (company facts only)** may enter the shared layer immediately as restated claims. Third-party and interview/process claims stay private until candidate corroboration (`corroboration_count >= 2` with ≥1 `candidate_report`).
- **URL ingest gate:** we check **robots.txt** for our user-agent before fetching. That is an **access gate, not a redistribution license.** Mitigations: facts-only storage (no verbatim page text), retained `source_url` / `source_title` attribution, private-by-default for third-party, candidate-corroboration before sharing interview claims. **Reviewed at v1 scale; needs a real legal opinion before scaling.**
- **Founder's own debriefs** = ideal seed fuel. Tag as "1 candidate report (founder)" (no inflated confidence). No NDA signed → cleared main hurdle; still filter each specific problem: *was I asked not to share it?* / *generic or their custom problem?* Describe custom ones in own words; lean to process shape over verbatim questions.
- **Caveat:** the legal-adjacent calls here (NDA/confidentiality, license verification, robots≠license) are a **risk map, not legal advice.** Fine at v1 scale; get a real opinion before scaling. Don't let "we settled it" harden into "definitely fine at scale."

## 12. Open questions

- Portfolio/personal tool vs. real multi-user product? (Recommended: start single-user, dogfood, harden only if earned.)
- If a business: commit to India wedge or B2B, not generic B2C.
- Acquisition channel for the $10K MRR goal (deferred).

## 13. Next build step

**Done (5 Aug 2026):** migration `0007_prep_corpus.sql` — `prep_chunks` with `(company, role, level, interview_type)` key, provenance, corroboration, pgvector; `applications.company_domain`; `prep-sources` bucket; Edge Functions `ingest-prep-source`, `prep-chat`, `save-prep-claims`; claim-based ingest (not verbatim pages); Save to prep; asymmetric cross-user promotion for interview claims.

**Still to close for live MVP:** confirm `OPENAI_API_KEY` is set as a hosted secret; redeploy the Vercel app if `VITE_AI_PROVIDER=edge` is not already live so prep chat uses the edge provider. Functions `ingest-prep-source`, `prep-chat`, `save-prep-claims` are deployed.

**Next after that:** confidence number in UI · Sentry/PostHog · landing claim fix. Catalog-first company/role/level (§16) and resume templates / Generate / Tailor PDF (§14) are shipped.

## 16. Catalog-first company / role / level (built)

**Problem this solved:** free-text company/role/level fragmented prep keys (Mid vs L3), put `(FTC)` into LinkedIn searches, and matched "Figma" in bios instead of current employer.

**Approach:** curated catalog + typeahead, not string-normalizer-first. Users pick when possible; Custom + request when not.

### What shipped (migration `0008_catalog.sql`)

- **Tables:** `catalog_levels`, `catalog_companies` (~40 high-interview names + domain + optional `linkedin_company_id`), `catalog_roles` (~28 families), `catalog_role_aliases` (swe/sde → Software Engineer), `catalog_requests` (user asks for missing company/role).
- **`applications`:** nullable `company_id` / `role_id` / `level_id`, plus `specialty` and `employment_type` (`full_time`|`contract`|`intern`|`other`). Display `company` / `role` / `level` text still stored.
- **Levels (generic, not L-bands):** Intern, Associate, Entry, Mid, Senior, Staff, Principal, Manager, Director, VP, C-level / Executive.
- **RoleDialog:** search-as-you-type Combobox for company and role; level select from ladder; specialty + employment type; "Request it" writes `catalog_requests`.
- **Prep keys:** Edge ingest / chat / save-claims use `prepKeysFromApplication` — catalog slug ids when FKs set (`google`, `software_engineer`, `mid`), else normalised custom text with `stripRoleNoise`.
- **Referrals:** [`linkedinSearch.ts`](src/lib/linkedinSearch.ts) — keywords = cleaned role + specialty; `currentCompany` facet when LinkedIn org id is seeded.

### Still later

- Grow catalog from requests; seed more LinkedIn org ids.
- Merge-votes when multiple users confirm custom duplicates (SWE vs SDE left as custom).
- Optional company-level hints ("At Google, Mid ≈ L3") — display only, never stored keys.
- Thin normalizer remains only for **customs** and legacy display matching.

## 14. Resume templates (Generate / Tailor)

**The question this answers:** "you found problems with my layout — fix them" and "tailor this for the JD." The original uploaded PDF usually cannot be fixed in place. PDF records marks on a page, not a flow of text. Editing someone's designed file quietly is worse than declining.

**So we generate a new document from structured fields** — the profile spine (Generate) or JD-edited fields (Tailor) — through ATS-safe HTML templates (**Classic** default, **Compact**), rendered to PDF by **server Chromium** on Vercel (`api/render-resume-pdf.ts`). No browser print, no LLM in render, no DOCX in v1.

**Entry points:** Profile **Generate resume** (template default on `profiles.default_template_id`) · Materials **Tailor for this JD** (`applications.template_id` override, `applications.tailored_resume` jsonb). Same renderer. Spine contact + education/projects/certs live on the profile (`0011`); tailor weights by JD relevance per item, never by dropping whole sections.

**Tailor integrity:** Edge Function `tailor-resume` may rephrase existing bullets toward JD language; never invents employers/titles/dates/skills. Missing JD skills get optional briefs (skill chip + 1–2 bullets on the application copy only). Follow-up `mode: "edit"` applies only instruction-named fields (`constrainEdit` locks the rest). Content-fidelity check vs the profile spine blocks download on hard employer/title/date drift; deliberate contact edits are soft warnings; `___` placeholders are allowed and highlighted in HTML/PDF.

**Replaced:** the hand-rolled browser PDF/DOCX rebuild (`pdf.ts`) that was reported badly formatted (5 Aug). That path is retired for Generate/Tailor; Word download is hidden until a later release.

**Still later:** four paid templates · LaTeX · DOCX download · structural vs content ATS score split · editing a Word original in place.

## 15. Rewriting the bullets (built, costs a model call)

**The question this answers:** "you told me this bullet has no measurable outcome — write the better one." Until now the rebuild deliberately refused to, on the grounds that rewriting means either inventing a number or leaving the sentence as weak as it was. That refusal was right about the risk and wrong about the options, and this is the third one: **rewrite the sentence, and leave the number blank.** "Cut duplicate records by `___`%" is a bullet its owner can finish in five seconds and defend in an interview. A figure the model chose is neither.

**One press rewrites the whole report**, not one press per finding — a single model call over the parse and the findings that are already stored, so the resume's pages are not paid for twice. About **$0.02–0.04**, roughly a third of an analysis. Every suggestion arrives as *original above, suggested below*, per finding and in one summary card, and nothing is applied by accepting it in the abstract: **accept, dismiss, or accept all**, then the template PDF is written from what was accepted.

**Accepted rewrites change the document and nothing else.** The profile spine is untouched, deliberately: a bullet with a blank in it belongs in a file its owner is about to finish editing, not in the store that every future tailoring reads from. The rewrite and the parse-to-profile review stay separate steps because they answer different questions.

**Three refusals hold the integrity line** from §3, and none of them trusts the prompt to do it:
- **It can only rewrite lines the resume actually contains.** A suggestion whose "original" doesn't match a stored line verbatim is discarded rather than guessed at, so a rewrite can never land on the wrong bullet.
- **New numbers are flagged, not accepted quietly.** The digits in a suggestion are compared against the digits in the original; anything new is shown with a warning and **excluded from "accept all"**, so it takes a deliberate individual click to keep.
- **It won't pretend to fix what wording can't.** Layout, page count and a missing home city are excluded from the pass — the rebuild handles the first two, and only the candidate knows the third.

**It works, and it costs about a cent.** First real call on 4 Aug 2026: **3,415 input tokens, 537 output, ~10 seconds, ≈$0.012** — an eighth of an analysis, because the resume's pages were already paid for. It rewrote the deduplication bullet exactly as designed: cut the filler, kept the real verb, and put `___` where the missing figure belongs with a note saying what to fill in. Nothing invented, nothing flagged.

**It returned one rewrite where the report named two rewritable lines, and chasing that found a worse bug somewhere else.** The finding it skipped quotes a bullet — "Contributed front-end features to a trade-route planning app…" — that **the parse never captured**, along with most of the bullets from two other roles. The rewriter may only touch lines the parse stored, so it could not have written that one; a second real pass with the prompt loosened returned the same single rewrite, which proves the prompt was never the constraint. The report's findings could see more of the resume than its own parse could, and since the parse is the only surviving record of the content, **an incomplete one is a resume with bullets missing** — which is what the rebuild's refusal-on-incomplete-parse guard exists to catch, and it caught that one.

**Fixed, and then re-run over a complete parse — where one rewrite turned out to be the right answer.** A fresh analysis on 4 Aug proved the parse-completeness rules hold: **nine bullets across four roles where there had been six**, and the trade-route line present under a project heading rather than lost. The rewrite pass over it (**3,904 in / 524 out, 9.7s, ≈$0.013**) again returned one — and this time by construction. Of seventeen findings, eleven are passes with nothing to fix, and of the six that remain, only one names a change to the words of a stored line. **The other five are not rewritable and refusing them is correct:** a missing city and a missing summary cannot be written without inventing facts, a non-standard heading and inconsistent date formats are structure rather than prose, and a skill listed twice wants deleting from a list, not rewriting. The ceiling here is a well-written resume, not a reticent model — which is a much better problem than the one it looked like.

---

## Version history

*Major changes only, going forward.*

- **7 Aug 2026 — Prod PDF download was dead on arrival.** `/api/render-resume-pdf` returned Vercel's `FUNCTION_INVOCATION_FAILED` (text/plain) for every call — the UI mapped that to "Could not render the PDF." Cause: shipping full `@sparticuz/chromium` blew the serverless size budget so the function never booted. Switched to `@sparticuz/chromium-min` + remote x64 pack; also read `VITE_SUPABASE_PUBLISHABLE_KEY` (the route had been looking for a non-existent `*_ANON_KEY`).
- **6 Aug 2026 — Follow-up edit on a tailored resume.** Materials gets a free-text "Tweak this version" box after fields exist. `tailor-resume` `mode: "edit"` + `constrainEdit` apply only instruction-named fields; does not re-tailor for the JD. Same `tailor` quota with confirm. Contact mismatches vs spine are soft so deliberate email/phone edits can still download.
- **5 Aug 2026 — Tailor spine gap closed + relevance weighting.** Migration `0011`: profile `phone`/`location`/`links`/`summary` plus `education`/`projects`/`certifications` (with line children). Onboarding confirm writes those sections; `loadSpine` queries them (with `resume_reports.parsed` fallback for older accounts). `TAILOR_SYSTEM` now weighs must-have vs nice-to-have per item, never by section, and logs omissions in `changes`; enrich briefs add only.
- **5 Aug 2026 — Tailor session persists on the application.** Leaving Materials mid–skill-gap (or after) used to drop summary/prompts from React state even though fields were saved, which pushed people to re-tailor and re-spend. `tailored_resume` now stores an envelope (fields + session); Re-tailor alone spends again, with a confirm.
- **5 Aug 2026 — Tailor was refusing every run.** The `tailor` allowance had no room in the ledger's feature check, so the usage insert failed and the request was refused before the model with a generic "Could not start this." Migration `0010` widens the check; `MaterialsTab` also had a render loop from a fresh `[]` fallback in `useProfileContext`.
- **5 Aug 2026 — resume templates + Generate / Tailor PDF.** Migration `0009`: `profiles.default_template_id`, `applications.template_id`, `applications.tailored_resume`. Classic/Compact HTML → Vercel Chromium PDF (`/api/render-resume-pdf`); hand-rolled PDF path retired for this surface. Edge `tailor-resume` + skill-gap elicitation + content-fidelity check. Quota feature `tailor`.
- **5 Aug 2026 — Google sign-in on the login screen.** Magic link stays as a fallback; OAuth uses `signInWithOAuth` with the same `${origin}/app` redirect allow-list. Provider secrets live in the Supabase Dashboard (hosted) or `supabase/.env` (local) — never under `VITE_`. `config.toml` enables `[auth.external.google]` for local GoTrue; hosted is configured in the Dashboard only (do not `config push` over live Site URL).
- **5 Aug 2026 — catalog-first company / role / level (option 3).** Free-text entry was fragmenting prep keys and poisoning LinkedIn search. Migration `0008`: catalog tables + aliases + requests; applications get FKs, specialty, employment_type. RoleDialog typeahead (Combobox) with Custom and request. Prep edge functions key off catalog slugs via `prepKeysFromApplication`. Referrals use cleaned titles + `currentCompany` when a LinkedIn org id is seeded. Levels are generic (Intern…Executive), never L1/L2/L3. Merge-votes for custom duplicates stay later. Local test applications/prep wiped for clean keys.
- **5 Aug 2026 — the prep panel was under-reporting itself, which looked like a broken chat.** Two Google roles side by side: one showed "1 source", the other "0 sources · Cold start" while its chat answered Google questions in detail and cited them. Nothing was actually broken — the count was reading `prep_sources` rows filed under one `application_id`, while retrieval matches any claim whose company and role are null or equal. A role therefore draws on two things the old count could never see: **company-scope sources added under a sibling role**, and **shared claims contributed by other accounts**, whose source row RLS hides permanently and correctly. The fix is honesty about grounding rather than any change to what gets retrieved: sibling company-scope sources are listed and counted on every role at that company, badged `company-wide` and with no delete button since they aren't that role's to remove; shared claims are reported as a count ("plus 2 shared claims about Google from other candidates") which never names anyone else's upload; and "Cold start" now requires that all three be empty. One duplication was accepted deliberately — `normaliseCompany` now exists in both the Vite bundle and the Deno functions, because sibling-matching in the browser has to agree with the `company` key the server writes, and the two runtimes cannot import each other. Both copies reduce `Google Inc.`, `GOOGLE, LLC` and `Google` to `google`; §10 records that they must be changed together.
- **5 Aug 2026 — prep chat stopped forgetting, and stopped denying the sources it had just cited.** Each turn was stateless: `prep-chat` got only the latest question, so follow-ups like "save it, it was in an interview" had nothing to resolve against. The system prompt also told the model to answer *only* from claims in a way that made it invent "I cannot access your index" even when citation pills were already on the answer. Now: last 8 turns travel with the question; claims are re-retrieved every turn; structured `{ answer, suggestedClaims }` so Save to prep comes from the exchange rather than dumping retrieved rows; labeled general coaching is allowed when claims don't cover (still never invent company-specific facts); citation pills and bare URLs in answers are clickable; Shift+Enter newlines in the ask box. Soft retrieval fallback when similarity ≥ 0.25 returns empty.
- **5 Aug 2026 — company prep RAG is real (claim-based, not verbatim pages).** Migration `0007`: `prep_chunks` + pgvector + `match_prep_chunks`, `applications.company_domain`, `prep-sources` bucket, `relevance_check` on the allowance ledger. Three Edge Functions: `ingest-prep-source` (URL via robots.txt → extract restated claims → embed; PDF/paste same extract path), `prep-chat` (retrieve + Haiku, answer only from claims), `save-prep-claims` (explicit Save to prep checklist). **Sharing is asymmetric:** company facts from first-party or news can be shared immediately; interview/process claims always start private and promote only at `corroboration_count >= 2` with ≥1 `candidate_report`. Consent copy scoped to the user's own notes/recaps. Legal note in §11: robots-allow is an access gate, not a redistribution license. Migration and all five functions are now on the hosted project. **The first ingest attempt failed for a reason that had nothing to do with any of the above, and took the résumé features down with it:** `ANTHROPIC_MODEL` had been switched to Haiku that morning to cut cost, and Haiku rejects `output_config.effort` outright while still accepting the `format` half of the same object — so analysis, rewrites, extraction and chat all began failing at once with "the model service could not process this request". Free, since a 400 is refused before tokens are counted, but invisible: the code logged the status and not the body, so a message Anthropic had spelled out by name had to be rediscovered with `curl`. `effort` is now assembled per-model rather than written inline, unknown models are assumed not to support it, and every non-`ok` model response logs its body. Both fixes are in `_shared/model.ts` so the next model swap is a config change rather than an outage.
- **5 Aug 2026 — the free tier got a number, and the plan behind it stopped being self-service.** Before opening the analyzer to a public signup page, the spend had to be bounded per person rather than only per account. The free tier is now **one resume analysis and one rewrite pass a month, five chat messages a day** — in [`_shared/plans.ts`](supabase/functions/_shared/plans.ts), which the React app imports too, so the number shown beside a button is by construction the number the function enforces. **The interesting part is what counting revealed.** The old cap was ten a day counted over `resume_reports` — i.e. over *successes* — which had been written down as a known defect since 4 Aug and was survivable only because the limit was loose: a call that reached Anthropic, was billed, and then failed left no row and never counted, so a repeatedly failing analysis billed straight past it. At one a month that is not a limit at all. So counting moved to `ai_usage` (migration `0006`), a ledger of **attempts**, written immediately before the model call — after everything that can refuse for free, and before the 200 that can no longer carry a refusal. A failed run now costs its owner the allowance, which is the deliberate side of the trade. Two holes found while building it, neither in the feature itself: `user_settings` had a `for all` policy and table-wide grants, so **any signed-in user could `PATCH` themselves to `pro`** — and narrowing only `update` would have left `delete` plus `insert` as the same escalation by another route, since the row could be dropped and re-inserted as `pro`. Both grants are now column-level, and `ai_usage` deliberately grants no `update` or `delete`, so a forged write can only spend an allowance rather than restore one. Proven against the local stack for nothing, on the stub: a first run spends one, a second is refused 429 with the sentence the user reads, a free 409 refusal costs nothing, the two allowances are independent, and all four escalation paths are denied at the database. The Anthropic monthly cap is set — **the first time the rule in §7 has actually been honoured before the calls rather than after them.**
- **5 Aug 2026 — the live site could not accept a resume, because nothing behind that screen had ever been deployed.** "Bucket not found" on the hosted app, and the audit behind it found the resume feature was **entirely absent from production**: 14 tables instead of 18, no storage bucket at all, neither Edge Function deployed. Every one of those had been written down as an open caveat since 4 Aug and none of it mattered until somebody used the real URL — which is the lesson worth keeping, since the docs were accurate and still didn't prevent it. `0004` and `0005` are now applied to the hosted project and verified equal to local: 18 tables, the private `resumes` bucket with its four per-user path policies, RLS on all four new tables, grants matching byte for byte. **Proven rather than assumed** — a session minted through the admin API without sending an email, then a real upload, insert and RLS-scoped read against the live project, cleaned up afterwards. A trap found on the way: the hosted project has **no migration ledger**, because `0001`–`0003` were pasted into the SQL editor by hand, so `supabase db push` would replay them and fail; these were applied the same manual way, and switching to `db push` needs `migration repair` first. **Deliberately still off: both Edge Functions, and `VITE_AI_PROVIDER` on Vercel** — the live site can now store a resume but not analyse one, which is the only thing keeping an open signup page from spending real money while the Anthropic cap is unset.
- **5 Aug 2026 — live magic links were going to localhost, and the rebuilt PDF is reported broken.** The hosted Supabase project still carried `site_url = http://localhost:5173` with only localhost allow-listed, so sign-in emails from the live site pointed at a machine that wasn't running. **The client was never wrong** — it sends `${window.location.origin}/app`, correct on Vercel — but **GoTrue silently discards an `emailRedirectTo` that isn't on the allow list and falls back to Site URL**, which makes a missing allow-list entry look exactly like a hardcoded localhost bug. Site URL is now the Vercel domain, with preview deploys and localhost allowed but no longer the fallback. Two traps recorded rather than left to be rediscovered: `supabase config push` would overwrite this with `config.toml`'s local values, and pointing `prepfor.me` at Vercel needs both fields updated. Separately and **not yet investigated: the PDF download from the rewrite flow comes out badly formatted** — logged with the discriminating tests rather than a guess, since a rebuild is supposed to discard the original layout and this may be that landing badly rather than a writer bug. See [§14](#-open-defect-the-pdf-download-comes-out-badly-formatted-reported-5-aug-2026-not-investigated).
- **4 Aug 2026 — the parse is complete, and the rewriter was right all along.** The first analysis run since the parse-completeness rules were written, non-streaming, four steps and thirteen heartbeats: **overall 85, seventeen findings, 80.6 seconds, ≈$0.10.** The rules hold. **Nine bullets across four roles where the previous parse had six**, one degree and five projects out of the newly `kind`-tagged array, and the bullet the old report could quote but not parse now sits under the project heading it belongs to — which closes the silent-content-loss bug found yesterday and gives the rebuild its first report it will not refuse. The rewrite pass over that parse (**≈$0.013**) returned one rewrite again, and this time that is the correct number rather than a symptom: eleven of the seventeen findings are passes, and of the six that are not, five cannot be fixed by rewriting a line at all — a missing city and a missing summary would have to be invented, a non-standard heading and mixed date formats are structure, and a duplicated skill wants deleting rather than rephrasing. **Still not raised by the report itself: a `parse` finding when bullets don't survive extraction** — this parse is complete, but nothing in the report would have said so if it weren't. The spend cap is still unset, and the JWT-expiry risk on the final insert is still open.
- **4 Aug 2026 — the analysis stopped streaming, because streaming it was killing it.** Two failures, one free and one not, and both were the kind you only find by running the thing. First: the three parse sections added earlier put the schema back over Anthropic's decoding-grammar ceiling — `invalid_request_error`, rejected in three seconds before billing. Repetition again, exactly as the seven category objects had been, and defining a shape once in source does nothing when what gets compiled is the schema as sent. Education, projects and certifications now travel as **one array tagged by `kind`**, split back apart in the validator so nothing downstream changed. Second, and billed: the schema then compiled, the answer streamed for 84 seconds, and the runtime **killed the isolate on its CPU limit** before a line of it was saved. The cause is per-frame cost charged by the runtime for reading the upstream body — a thinking-heavy answer arrives in thousands of frames, and the ceiling is around 1,500 — while our own JavaScript accounted for **365 ms** of the budget it had supposedly exhausted. I misdiagnosed it twice before measuring it. So the analysis is now **one non-streaming call**: four honest steps, and through the long model wait a clock rather than an invented percentage, with a paler fill that creeps toward the next step in proportion to elapsed-against-typical and stops dead there. The rewrite pass, whose answers are a few hundred frames, still streams a genuinely moving bar. The stub can now be told to be slow — `STUB_WAIT_MS`, `STUB_THINK_FRAMES`, `STUB_CHUNK` — which is how both causes were confirmed for nothing after the fact, and how the fix was proven over a real 90-second wait before anything else was spent. **Also found and not yet fixed: every database write in the function runs under the caller's JWT, so an analysis starting near the end of a token's hour finishes into `JWT expired` and loses a report that was paid for.** The spend cap is still unset.
- **4 Aug 2026 — the report can now rewrite the bullets it criticises.** A second Edge Function, `improve-resume`, and migration `0005` (`resume_improvements`, `resume_edits`). One press rewrites the whole report in a single text-only call over the parse and findings already stored — **$0.02–0.04**, versus thirty-odd cents if each finding got its own call — and every suggestion is shown original-above-rewrite-below to accept, dismiss, or accept in bulk. **The integrity rules are enforced in code, not asked for in the prompt:** a rewrite whose original doesn't match a stored line verbatim is discarded rather than guessed at, a rewrite that introduces a figure the original never stated is flagged and excluded from "accept all", and a bullet missing a metric comes back with `___` to fill rather than a number to defend. Accepted rewrites feed the rebuilt document only — never the profile — because a bullet with a blank in it belongs in a file its owner is about to finish, not in the store every future tailoring reads. Two bugs found by running it rather than reading it: a fresh set used to stack on top of the old one instead of replacing it, and the apply step crashed outright on a report stored before the parse covered education and projects — a legacy parse's absent sections are now passed through as absent, since inventing empty ones would let the rebuild believe someone simply has no degree. **Now run for real: 3,415 in / 537 out, ~10 seconds, ≈$0.012 — an eighth of an analysis — and the schema compiled first time.** It produced one rewrite of textbook quality, and the second it appeared to miss turned out to be a bullet **the analysis never parsed** — the report quotes lines the parse doesn't contain, which is a silent-content-loss bug in the analyzer rather than a reticent rewriter, and the rebuild's incomplete-parse guard is what currently contains it. **The spend cap is still unset.** Also renumbers the corpus migration from `0005` to `0006`. See [§15](#15-rewriting-the-bullets-built-costs-a-model-call).
- **4 Aug 2026 — the resume can be rebuilt, and the parse now covers the whole document.** The report gained a `layout` classification (`single_column_text` / `multi_column` / `graphical` / `scanned`), and the parse gained the sections it had been ignoring — summary, education, projects, certifications — because a rebuild written from an incomplete parse silently drops somebody's degree. Education dates are kept **verbatim** rather than normalised, both to preserve what the page said and because nullable fields are rationed by the schema's union budget. On top of that: a DOCX writer, a PDF writer, and a ZIP writer, all by hand and all dependency-free, adding **21 kB** to the bundle where a document library would have added several hundred. Verified against macOS's own PDF engine, which caught a genuinely invisible bug — the fill colour was inverted, so body text was being painted white; it extracted perfectly and rendered as a blank page, so nothing short of looking at it would have found it. A report stored before this change is **refused** for rebuilding rather than rendered short, and a PDF is refused for any resume using characters the base-14 fonts cannot write, with the Word file offered instead. **Answered since, and the answer was no:** the enlarged schema was rejected outright for grammar size, for free, and the three sections had to be collapsed into one `kind`-tagged array — see the entry above. See [§14](#14-rebuilding-the-resume-built-free-to-run).
- **4 Aug 2026 — the analyzer works.** Three real calls, about twenty cents between them, and the third produced a report: **overall 86, seven categories, five ranked fixes, four roles and thirty skills parsed, ~70 seconds, ~$0.09.** The two failures before it were both worth having. A schema whose seven repeated category objects compiled into a decoding grammar Anthropic refuses outright — collapsed into one array with an `id` enum, and rejected free. Then a `max_tokens` sized for the answer alone, which Sonnet 5's default adaptive thinking spent on reasoning before truncating the report: billed in full, unparseable, and fixed with a 20k budget at `medium` effort plus a three-finding ceiling per category. The analysis now streams NDJSON and the UI draws a **real progress bar** — twelve milestones, each one a section the model has actually finished writing, so a stalled run looks stalled instead of looking busy. (Superseded later the same day: streaming the model's own response turned out to cost more CPU than an edge isolate is given, and the bar is now four steps and a clock.) The plumbing is provable without spending anything, via `ANTHROPIC_BASE_URL` and a local stub that speaks Anthropic's stream format. Also corrected here: `early termination` in the local function log is the worker being recycled on a timer, not a request being killed — it was blamed for one of the failures above and had nothing to do with it. **Still open: the spend cap, and the fact that over half a run's cost is thinking tokens nobody reads.**
- **4 Aug 2026 — analysis split off from upload, and local dev pointed at the real analyzer.** Uploading now stores the PDF and stops on `uploaded`; the model call is a separate explicit press, because the only code path that spends money should never be reachable from a file picker. The client also stopped writing `resumes.status` altogether — the Edge Function is the only writer. That fixed a real defect: the old client `catch` wrote `failed` on every thrown error, including the function's own 409 "already being analyzed" refusal, which released the in-flight lock that refusal existed to protect. Press once and get refused, row flips to `failed`, press again and get billed. A stalled run is now escapable after three minutes instead of spinning forever. And `.env.development.local` now sets `VITE_AI_PROVIDER=edge` with the key in `supabase/.env.local`, which **reverses the standing claim that a dev machine cannot spend money by accident** — it can, on one press, today, with no cap set. Also newly recorded rather than newly true: the daily cap counts saved reports, so it limits successful analyses, not spend. **Still no real model call, `0004` still not on the hosted project, function still not deployed, its secret still not set, cap still unset.**
- **4 Aug 2026 — base resume upload and ATS analysis.** The first server-side provider: a private per-user Storage bucket, migration `0004`, and `analyze-resume`, a Supabase Edge Function that sends the PDF to Claude as a document block and gets back a seven-category ATS report and a structured parse in one call. The parse is reviewed item by item before any of it touches the profile spine, so tailoring finally has real bullets behind it. The client picks its provider per environment and defaults to the local one, which returns a labelled sample, and no sample is ever stored. Closes [BUILD_PLAN.md](BUILD_PLAN.md) Part B steps 2 and 4, and pushes the corpus migration from `0004` to `0005`. **Caveat, deliberately loud: no real model call has been made, the migration is not on the hosted project, the function is not deployed, and the spend cap is still unset.**
- **2 Aug 2026 — CRUD skeleton shipped and deployed.** Replaced the hardcoded prototype with a real backend: Supabase Postgres schema across 14 tables with RLS on every one, magic-link auth with a session guard, React Router URLs, a TanStack Query data layer, real form inputs that persist, metrics derived from actual rows, and an `AiProvider` seam with a local implementation. First deploy to Vercel at prep-for-me.vercel.app. Discovered and fixed the Supabase Data API grant change (`0003_grants.sql`). No AI, resume parsing, or job feeds yet.
- **2 Aug 2026 — baseline.** Source-of-truth doc established. Captures project definition, tech stack, scope, and the settled data-sourcing/integrity/confidence/founder-debrief decisions from the strategy session. No build work started beyond the CRUD skeleton.
